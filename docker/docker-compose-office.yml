services:
  office:
    container_name: mep-office
    image: onlyoffice/documentserver:7.1.1
    ports:
      - "8701:80"
    environment:
      TZ: Asia/Shanghai
      JWT_ENABLED: "false"
    volumes:
      - ${DOCKER_VOLUME_DIRECTORY:-.}/office/mep:/var/www/onlyoffice/documentserver/sdkjs-plugins/mep
    command: bash -c "supervisorctl restart all"
    restart: on-failure

