---
description: 开发工作流、提示词记录与增量部署规则
globs:
alwaysApply: true
---

# 开发工作流（严格按顺序执行）

每次开发任务必须按 **部署前清理 → 本地开发 → 部署远程服务器 → 新建分支上传 GitHub** 四步顺序执行，不得跳过。

1. **部署前清理**: 按下方"部署前清理规则"执行代码清理
2. **本地开发**: 在 `/Users/dingwutao/Dev/Project/HYSys` 完成代码修改，确认无误
3. **部署远程服务器**: 按下方增量部署规则部署到 `39.104.208.248`，验证服务正常
4. **上传 GitHub**: 新建功能分支，提交并推送到 `https://github.com/stonedingwt/MEPSS.git`

# 测试规则

- **开发完成后不使用浏览器测试**：所有开发任务完成并部署后，不需要通过 MCP 浏览器工具进行页面测试验证。直接告知用户已完成部署即可。
- 仅在用户明确要求浏览器测试时才使用浏览器工具。

# 项目关键信息

- **本地代码目录**: `/Users/dingwutao/Dev/Project/HYSys`
- **GitHub 仓库**: `https://github.com/stonedingwt/MEPSS.git`
- **远程服务器 IP**: 39.104.208.248
- **SSH 密码**: `stoneding!@34`
- **SSH 命令**: `sshpass -p 'stoneding!@34' ssh -o StrictHostKeyChecking=no -o PubkeyAuthentication=no root@39.104.208.248`

## HYSys 航运系统（主项目，3 个容器）

- **访问地址**: `http://39.104.208.248:3002`（工作台）、`http://39.104.208.248:3002/sysadmin`（管理端）
- **管理员**: `admin` / `hysys2026`
- **后端容器**: hysys-backend（API 端口 7861, limit 1.5g）、hysys-backend-worker（Celery, limit 1g）
- **前端容器**: hysys-frontend（Nginx 端口 3002, limit 128m）
- **基础设施**: 共享 MEP 的 MySQL/Redis/ES/Milvus/MinIO（不再有独立基础设施容器）
- **Docker Compose 目录**: `/opt/hysys/docker/`
- **Docker Compose 项目名**: `hysys`（启动命令: `docker compose -p hysys up -d`）
- **Docker 网络**: `docker_default`（与 MEP 共享）
- **数据库**: hysys（共享 mep-mysql:3306 中的 hysys 库）
- **Redis**: DB 3/4（共享 mep-redis:6379）
- **Milvus partition_suffix**: 2（MEP 为 1）

## MEP 原景系统（遗留，含共享基础设施）

- **后端容器**: mep-backend（limit 1.5g）、mep-backend-worker（limit 2.5g）
- **前端容器**: mep-frontend（端口 3001, limit 128m）
- **基础设施**: mep-mysql（3306, limit 768m）、mep-redis（6379, limit 256m）、mep-es（9200, limit 1g）、mep-milvus-standalone（19530, limit 512m）、mep-milvus-minio（9100, limit 256m）、mep-milvus-etcd（limit 128m）
- **其他**: mep-office（8701）
- **Docker Compose 目录**: `/opt/mep/docker/`

# 提示词记录规则

每次用户发送提示词并完成执行后，**必须**在 `PROMPT_LOG.md` 文件末尾追加一条新记录，格式如下：

```
---

## #N — 简要标题

- **提示词**: （用户原文）
- **发送时间**: YYYY-MM-DD HH:MM
- **执行结果**: ✅ 完成 / ⚠️ 部分完成 / ❌ 失败
  - （简要列出执行的关键操作）
- **修改文件**:
  - （列出本次修改过的所有文件路径）
- **完成时间**: YYYY-MM-DD HH:MM
```

要求：
- `#N` 为递增编号，接续文件中已有的最大编号
- 发送时间取用户发送消息时的时间（CST 北京时间）
- 修改文件列出所有被新建、编辑的本地文件相对路径
- 每次任务完成后立即更新，不要遗漏

# 部署前清理规则（每次部署和上传代码前必须执行）

每次部署以及上传最新代码前，**必须先执行以下清理操作**：

```bash
# 1. 清理 Python 缓存
cd /Users/dingwutao/Dev/Project/HYSys
find src/backend -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null
find src/backend -name '*.pyc' -delete 2>/dev/null

# 2. 清理前端构建产物
rm -rf src/frontend/client/build/ src/frontend/platform/build/

# 3. 清理 Python/前端工具缓存
rm -rf .pytest_cache .ruff_cache .mypy_cache .eslintcache .cache

# 4. 清理 Cursor 无效临时缓存
rm -rf /Users/dingwutao/.cursor/projects/var-folders-*

# 5. 清理服务器临时文件
sshpass -p 'stoneding!@34' ssh -o StrictHostKeyChecking=no -o PubkeyAuthentication=no root@39.104.208.248 'rm -rf /tmp/frontend_update /tmp/platform_update /tmp/*.py /tmp/*.json'
```

**不要删除**的文件：
- `src/frontend/*/public/vditor/dist/` — 第三方 vendor 资源
- `node_modules/` — 依赖目录（除非需要全量重装）
- `.cursor/rules/` 和 `.cursor/skills/` — 项目规则和技能

# 增量部署规则

部署目标为服务器 `39.104.208.248`。

## HYSys 后端部署（hysys-backend / hysys-backend-worker 容器）

只将**本次实际修改过的后端文件**复制到容器内对应路径，然后重启容器。

```bash
sshpass -p 'stoneding!@34' scp -o StrictHostKeyChecking=no -o PubkeyAuthentication=no <本地修改的文件> root@39.104.208.248:/tmp/
sshpass -p 'stoneding!@34' ssh -o StrictHostKeyChecking=no -o PubkeyAuthentication=no root@39.104.208.248 '
  docker cp /tmp/<文件名> hysys-backend:/app/<对应路径>
  docker cp /tmp/<文件名> hysys-backend-worker:/app/<对应路径>
  docker restart hysys-backend hysys-backend-worker
'
```

**禁止**：全量替换后端代码目录。

## HYSys 前端部署（hysys-frontend 容器）

Nginx 容器中两个前端子应用的路径：
- **client (工作台)**: `/usr/share/nginx/html/client/`，URL 前缀 `/`（根路径）
- **platform (管理端)**: `/usr/share/nginx/html/platform/`，URL 前缀 `/sysadmin`

### Client 工作台部署

```bash
# 1. 本地构建
cd /Users/dingwutao/Dev/Project/HYSys/src/frontend/client && npm run build

# 2. 上传构建产物
sshpass -p 'stoneding!@34' ssh -o StrictHostKeyChecking=no -o PubkeyAuthentication=no root@39.104.208.248 'mkdir -p /tmp/frontend_update'
sshpass -p 'stoneding!@34' scp -o StrictHostKeyChecking=no -o PubkeyAuthentication=no -r build/* root@39.104.208.248:/tmp/frontend_update/

# 3. 部署到 HYSys 前端容器
sshpass -p 'stoneding!@34' ssh -o StrictHostKeyChecking=no -o PubkeyAuthentication=no root@39.104.208.248 '
  docker cp /tmp/frontend_update/. hysys-frontend:/usr/share/nginx/html/client/
  docker exec hysys-frontend nginx -s reload
  rm -rf /tmp/frontend_update
'
```

### Platform 管理端部署

```bash
# 1. 本地构建
cd /Users/dingwutao/Dev/Project/HYSys/src/frontend/platform && npm run build

# 2. 上传构建产物
sshpass -p 'stoneding!@34' ssh -o StrictHostKeyChecking=no -o PubkeyAuthentication=no root@39.104.208.248 'mkdir -p /tmp/platform_update'
sshpass -p 'stoneding!@34' scp -o StrictHostKeyChecking=no -o PubkeyAuthentication=no -r build/* root@39.104.208.248:/tmp/platform_update/

# 3. 部署到 HYSys 前端容器
sshpass -p 'stoneding!@34' ssh -o StrictHostKeyChecking=no -o PubkeyAuthentication=no root@39.104.208.248 '
  docker cp /tmp/platform_update/. hysys-frontend:/usr/share/nginx/html/platform/
  docker exec hysys-frontend nginx -s reload
  rm -rf /tmp/platform_update
'
```

**禁止**：将整个前端源码目录打包上传覆盖服务器源码，除非用户明确要求全量部署。
**注意**：不要混淆两个子应用的部署路径，client → `client/`，platform → `platform/`。

## HYSys Docker Compose 操作

```bash
# 启动全部 HYSys 服务
sshpass -p 'stoneding!@34' ssh root@39.104.208.248 'cd /opt/hysys/docker && docker compose -p hysys up -d'

# 停止全部 HYSys 服务
sshpass -p 'stoneding!@34' ssh root@39.104.208.248 'cd /opt/hysys/docker && docker compose -p hysys down'

# 查看 HYSys 服务状态
sshpass -p 'stoneding!@34' ssh root@39.104.208.248 'docker ps --filter "name=hysys"'
```

## 部署前检查清单

- [ ] 执行部署前清理（见上方清理规则）
- [ ] 确认本次修改文件列表
- [ ] 只上传修改过的文件
- [ ] 构建后验证关键页面可访问
- [ ] 将部署结果记录到 `PROMPT_LOG.md`
