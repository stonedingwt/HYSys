---
description: 开发工作流、提示词记录与增量部署规则
globs:
alwaysApply: true
---

# 开发工作流（严格按顺序执行）

每次开发任务必须按 **本地开发 → 部署远程服务器 → 新建分支上传 GitHub** 三步顺序执行，不得跳过。

1. **本地开发**: 在 `/Users/dingwutao/Dev/Project/MEP_Dev` 完成代码修改，确认无误
2. **部署远程服务器**: 按下方增量部署规则部署到 `39.104.208.248`，验证服务正常
3. **上传 GitHub**: 新建功能分支，提交并推送到 `https://github.com/stonedingwt/MEPSS.git`

# 测试规则

- **开发完成后不使用浏览器测试**：所有开发任务完成并部署后，不需要通过 MCP 浏览器工具进行页面测试验证。直接告知用户已完成部署即可。
- 仅在用户明确要求浏览器测试时才使用浏览器工具。

# 项目关键信息

- **本地代码目录**: `/Users/dingwutao/Dev/Project/MEP_Dev`
- **GitHub 仓库**: `https://github.com/stonedingwt/MEPSS.git`
- **远程服务器 IP**: 39.104.208.248（新服务器，已从 47.96.187.216 迁移）
- **SSH 密码**: `stoneding!@34`
- **SSH 命令**: `sshpass -p 'stoneding!@34' ssh -o StrictHostKeyChecking=no -o PubkeyAuthentication=no root@39.104.208.248`
- **旧服务器**: 47.96.187.216（已弃用，不再部署更新）
- **后端容器**: mep-backend、mep-backend-worker
- **前端容器**: mep-frontend
- **Docker Compose 目录**: /opt/mep/docker/

# 提示词记录规则

每次用户发送提示词并完成执行后，**必须**在 `PROMPT_LOG.md` 文件末尾追加一条新记录，格式如下：

```
---

## #N — 简要标题

- **提示词**: （用户原文）
- **发送时间**: YYYY-MM-DD HH:MM
- **执行结果**: ✅ 完成 / ⚠️ 部分完成 / ❌ 失败
  - （简要列出执行的关键操作）
- **修改文件**:
  - （列出本次修改过的所有文件路径）
- **完成时间**: YYYY-MM-DD HH:MM
```

要求：
- `#N` 为递增编号，接续文件中已有的最大编号
- 发送时间取用户发送消息时的时间（CST 北京时间）
- 修改文件列出所有被新建、编辑的本地文件相对路径
- 每次任务完成后立即更新，不要遗漏

# 增量部署规则

部署目标为**新服务器 `39.104.208.248`**。旧服务器 `47.96.187.216` 已弃用，**不再部署任何更新**。

## 后端部署（mep-backend / mep-backend-worker 容器）

只将**本次实际修改过的后端文件**复制到容器内对应路径，然后重启容器。

```bash
# 示例：只部署修改过的文件
sshpass -p 'stoneding!@34' scp -o StrictHostKeyChecking=no -o PubkeyAuthentication=no <本地修改的文件> root@39.104.208.248:/tmp/
sshpass -p 'stoneding!@34' ssh -o StrictHostKeyChecking=no -o PubkeyAuthentication=no root@39.104.208.248 '
  docker cp /tmp/<文件名> mep-backend:/app/<对应路径>
  docker cp /tmp/<文件名> mep-backend-worker:/app/<对应路径>
  docker restart mep-backend mep-backend-worker
'
```

**禁止**：全量替换后端代码目录。

## 前端部署（mep-frontend 容器）

1. **本地构建**: 在本地 `src/frontend/client/` 执行 `npm run build`
2. **SCP 构建产物**: 将 `build/` 目录上传到服务器
3. **docker cp 到容器**: 复制到 Nginx 容器并 reload

```bash
# 1. 本地构建
cd /Users/dingwutao/Dev/Project/MEP_Dev/src/frontend/client && npm run build

# 2. 上传构建产物
sshpass -p 'stoneding!@34' ssh -o StrictHostKeyChecking=no -o PubkeyAuthentication=no root@39.104.208.248 'mkdir -p /tmp/frontend_update'
sshpass -p 'stoneding!@34' scp -o StrictHostKeyChecking=no -o PubkeyAuthentication=no -r build/* root@39.104.208.248:/tmp/frontend_update/

# 3. 部署到容器
sshpass -p 'stoneding!@34' ssh -o StrictHostKeyChecking=no -o PubkeyAuthentication=no root@39.104.208.248 '
  docker cp /tmp/frontend_update/. mep-frontend:/usr/share/nginx/html/client/
  docker exec mep-frontend nginx -s reload
  rm -rf /tmp/frontend_update
'
```

**禁止**：将整个 `src/frontend/client/` 目录打包上传覆盖服务器源码，除非用户明确要求全量部署。

## 部署前检查清单

- [ ] 确认本次修改文件列表
- [ ] 只上传修改过的文件
- [ ] 构建后验证关键页面可访问
- [ ] 将部署结果记录到 `PROMPT_LOG.md`
