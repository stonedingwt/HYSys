---
description: 上下文管理规则 — 当对话较长时主动进行总结并提醒用户
globs:
alwaysApply: true
---

# 上下文管理规则

## AI 记忆机制说明

- **对话上下文窗口**（短期）：当前对话的消息和工具调用结果。有大小限制，超长对话会被自动摘要压缩，丢失细节。
- **对话记录文件**（中期）：完整对话文本保存在 `agent-transcripts/` 中，上下文被压缩后可通过搜索恢复细节，仅在当前对话有效。
- **`.cursor/rules/` 规则文件**（长期）：跨所有对话持久生效，是唯一的"长期记忆"。

## 上下文监控规则

当满足以下任一条件时，**必须主动提醒用户**：

1. 对话轮次超过 **20 轮**（用户消息 + 助手回复各算一轮）
2. 单次对话中执行了大量工具调用（超过 **50 次**工具调用）
3. 对话涉及 **3 个以上不同的功能模块或任务**
4. 已经出现过一次 `[Previous conversation summary]`（说明上下文已经被压缩过一次）

## 提醒时必须执行的操作

1. **生成当前任务状态摘要**，包含：
   - 已完成的任务列表
   - 进行中的任务及当前状态
   - 待完成的任务
   - 本次修改过的所有文件路径
   - 关键的技术决策和上下文

2. **明确告知用户**：
   - 当前对话已较长，上下文可能即将或已经被压缩
   - 建议是否开启新对话继续剩余工作
   - 如果开启新对话，用户可以将摘要作为新对话的上下文提供

3. **将摘要写入 PROMPT_LOG.md**（遵循提示词记录规则），确保跨对话可追溯
