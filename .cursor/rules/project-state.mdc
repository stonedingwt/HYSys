---
description: HYSys 项目状态记忆文件 - AI 在每次重要对话结束时应更新此文件
globs:
alwaysApply: true
---

# HYSys 航运行业 AI 系统 项目全景

## 项目概述

HYSys 是基于 MEP 平台打造的航运行业 AI 系统，独立代码仓库（`https://github.com/stonedingwt/HYSys.git`）。与 MEP_Dev（`https://github.com/stonedingwt/MEPSS.git`）是两个独立项目，仅共享同一台部署服务器及其基础设施。

## 架构概览

- **后端**: FastAPI + SQLModel + Celery + Redis，入口 `mep/main.py`，端口 7861
- **前端**: React + TypeScript + Vite，两个子应用 (client 工作台 + platform 管理端)
- **数据库**: MySQL 8.0，ORM 为 SQLModel/SQLAlchemy，数据库名 `hysys`
- **向量检索**: Milvus + Elasticsearch
- **对象存储**: MinIO
- **LLM**: DashScope API (qwen-max/qwen-plus/qwen-turbo)
- **部署**: Docker 容器集群，HYSys 与 MEP 共享基础设施（MySQL/Redis/ES/Milvus/MinIO）

## 部署信息

### 服务器资源

- **服务器**: 39.104.208.248（8核 Intel Xeon / 14 GiB RAM / 4 GiB Swap / 197G 磁盘）
- **容器总数**: 13 个（MEP 10 + HYSys 3）
- **内存预算**: 全部容器设有内存限制，总上限约 10 GiB，留 4 GiB 余量 + 4 GiB Swap

### HYSys（主系统，3 个容器）

- **远程服务器**: 39.104.208.248
- **访问地址**: `http://39.104.208.248:3002`（管理端）、`http://39.104.208.248:3002/workspace/`（工作台）
- **管理员**: `admin` / `hysys2026`
- **SSH**: `sshpass -p 'stoneding!@34' ssh -o StrictHostKeyChecking=no -o PubkeyAuthentication=no root@39.104.208.248`
- **后端容器**: hysys-backend (API 7861, limit 1.5g)、hysys-backend-worker (Celery, limit 1g)
- **前端容器**: hysys-frontend (Nginx 3002, limit 128m)
- **基础设施**: 共享 MEP 的 MySQL/Redis/ES/Milvus/MinIO（见下方数据隔离）
- **Docker Compose 目录**: `/opt/hysys/docker/`
- **Docker Compose 项目名**: `hysys`
- **Docker 网络**: `docker_default`（与 MEP 共享）
- **Nginx 路由**: `/` → platform，`/sysadmin` → platform，`/api` → hysys-backend:7861

### 数据隔离策略（HYSys 与 MEP 共享基础设施）

- **MySQL**: 共享 mep-mysql (3306)，HYSys 使用 `hysys` 库，MEP 使用 `mep` 库
- **Redis**: 共享 mep-redis (6379)，HYSys 使用 DB 3/4，MEP 使用 DB 1/2
- **Milvus**: 共享 mep-milvus-standalone (19530)，HYSys partition_suffix=2，MEP partition_suffix=1
- **MinIO**: 共享 mep-milvus-minio (9000)，HYSys bucket=hysys，MEP bucket=mep
- **ES**: 共享 mep-es (9200)，索引名由应用逻辑区分

### MEP（遗留系统，10 个容器含共享基础设施）

- **后端容器**: mep-backend (7860, limit 1.5g)、mep-backend-worker (limit 2.5g)
- **前端容器**: mep-frontend (3001, limit 128m)
- **基础设施**: mep-mysql (3306, limit 768m)、mep-redis (6379, limit 256m)、mep-es (9200, limit 1g)、mep-milvus-standalone (19530, limit 512m)、mep-milvus-minio (9100, limit 256m)、mep-milvus-etcd (limit 128m)
- **其他**: mep-office (8701)
- **Docker Compose 目录**: `/opt/mep/docker/`

## 核心 API 模块 (后端 `mep/api/v1/`)

| 文件 | 前缀 | 功能 |
|------|------|------|
| `task_center.py` | `/task-center` | 任务中心：任务 CRUD/转交/关注/表单/日志 |
| `biz_forms.py` | `/biz-forms` | 三表编辑：跟单表/BOM表/打样单 + 完整性检查 |
| `scheduled_task.py` | `/scheduled_task` | 定时任务管理 |
| `parse_rules.py` | `/parse-rules` | 解析规则 + Prompt 版本管理 + 模型发现 |
| `db_meta.py` | `/db/meta` | 数据库元数据 (表/列/注释) |
| `master_data.py` | (动态) | 主数据 CRUD (客户/供应商等) |
| `workstation.py` | (工作台) | AI助手对话/知识库查询 |

## 核心业务数据模型 (`mep/database/models/`)

| 文件 | 表 | 说明 |
|------|------|------|
| `biz_tables.py` | `biz_follow_up`, `biz_bom`, `biz_bom_detail`, `biz_sample`, `biz_sample_ratio`, `biz_sample_material` | 三表系统 (跟单/BOM/打样) |
| `task_center.py` | `task`, `task_form`, `task_focus`, `task_update_log` | 任务中心 |
| `parse_rule.py` | `parse_rule`, `prompt_version` | 解析规则 |
| `parsing_log.py` | `parsing_log` | 解析日志 |
| `master_data.py` | `master_customer`, `master_supplier` 等 | 主数据 |
| `scheduled_task.py` | `scheduled_task`, `scheduled_task_log` | 定时任务 |
| `packing_spec.py` | `packing_spec` | 装箱规格 |

## 核心业务服务 (`mep/core/biz/`)

| 文件 | 功能 |
|------|------|
| `auto_extract.py` | 自动提取: 解析 → LLM 推断 → 主数据匹配 → 三表填充 + 图片提取 |
| `data_validator.py` | 数据校验修复: 格式验证 + LLM 自动修复 + 钉钉通知 |
| `task_dedup.py` | 任务去重: PO 号查重 + 差异对比 + 自动更新 |
| `knowledge_sync.py` | 知识同步: 三表 → Markdown → 赛乐文档中心 |
| `image_sync.py` | 首图同步: MinIO → 金蝶附件上传 |
| `kingdee_sync.py` | 金蝶同步: BOM/打样单 → Web API Save/Submit |

## AI/模型 (`mep/core/ai/`)

| 模块 | 功能 |
|------|------|
| `model_registry.py` | 多模型调度: OCR 三级降级 (PaddleOCR→qwen-vl-ocr→qwen-vl-max) + LLM 自动选优 |
| `llm/` | LLM 客户端 (OpenAI 兼容/通义/DeepSeek) |
| `embeddings/` | 向量嵌入 |
| `rerank/` | 重排序 |

## 文档解析 (`mep/core/documents/`)

| 文件 | 功能 |
|------|------|
| `parser.py` | 统一入口：按供应商选择 handler |
| `generic_handler.py` | 通用格式解析 |
| `supplier_138731.py` | HKM (Hunkemöller) 专用解析 |
| `base.py` | 基础工具方法 (含 `is_total_row`) |
| `packing_list.py` | 装箱单生成 |

## 前端关键页面

### 工作台 (`src/frontend/client/`)
- **任务中心** `WsTaskCenter/`: TaskList, TaskDetail, BizFormEditor, StyleImageGallery, TaskChat
- **跟单助手** `WsOrderAssistant/`: 文件上传、解析日志
- **报价助手** `WsCostBudget/`: 报价表单、历史记录
- **销售订单** `WsSalesOrder/`: 订单列表
- **赛乐助手** `appChat/`: ChatView, ChatMessages, ChatInput

### 管理端 (`src/frontend/platform/`)
- **系统管理** `SystemPage/`: 用户/角色/数据库/数据字典
- **定时任务** `ScheduledTaskPage/`: 定时任务列表/编辑
- **知识库** `KnowledgePage/FilesPage/`: 文档管理
- **模型** `Management/`: LLM 模型配置
- **工作流** `FlowPage/`: 可视化工作流编辑

## 关键业务流程

### 跟单助手完整流程
```
文件上传 → MD5去重 → PaddleOCR(+降级) → LLM结构化(+降级)
→ OrderParser.parse → SalesOrderDao.import_orders
→ 任务去重(PO号) → 创建Task → populate_three_tables(跟单表+BOM+打样单)
→ validate_and_repair → 知识库同步 → 钉钉通知
```

### 报价助手流程
```
用户填写报价 → 保存记录 → 标记最终报价
```

## 前端 URL 路由

| 子应用 | 路径前缀 | Vite BASE_URL | Nginx 映射 |
|---|---|---|---|
| **client (工作台)** | `/` (根路径) | `''` | `/usr/share/nginx/html/client` |
| **platform (管理端)** | `/sysadmin` | `/sysadmin` | `/usr/share/nginx/html/platform` |

- 工作台访问地址: `http://39.104.208.248:3002`
- 管理端访问地址: `http://39.104.208.248:3002/sysadmin`
- 登录方式: 用户名密码（默认）
- 公司名称/系统名称: 通过管理端 主题配色 页面配置，存储在 ThemeStyle.branding

## 约定

- 数据库所有表和列必须有 comment
- LLM 调用遵循最小 token 原则
- 增量部署：只部署修改过的文件
- 开发后不自动浏览器测试
- **每次部署前必须清理**：`__pycache__`、构建产物、工具缓存、Cursor 临时缓存、服务器 `/tmp/` 临时文件
- **金蝶集成已移除**：所有金蝶相关代码、API、定时任务已删除
- **已移除功能**：跟单助手、报价助手、销售订单、装箱单规格（#65 删除）
- **GitHub 同步**：每 15 轮对话推送到 `git@github.com:stonedingwt/HYSys.git` main 分支（remote: `hysys`）
- **MEP_Dev 是独立项目**：`https://github.com/stonedingwt/MEPSS.git` 为独立代码仓库，与 HYSys 仅共享同一台部署服务器

## 最后更新
- **时间**: 2026-03-08
- **操作**: 修正项目关系 — HYSys 与 MEP_Dev 为两个独立项目/独立仓库，仅共享同一台部署服务器
